/**
 * Send message to Slack through Webhook
 * @param  {String} webhookURL Webhook URL to send message to slack
 * @param  {JSON} body JSON request to send
 */
const sendWebhook = (webhookURL, body) => {};

module.exports = sendWebhook;
